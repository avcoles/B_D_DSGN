# Dependencies — reinstalled from package-lock.json, never committed
node_modules/

# Build output — regenerated by the deploy workflow
dist/

# Logs and editor noise
*.log
.DS_Store
.vscode/
.idea/

# Local environment files
.env
.env.local
.env.*.local

# Personal Claude Code settings. launch.json is shared config and stays; this
# one is per-machine and contains local paths.
.claude/settings.local.json
